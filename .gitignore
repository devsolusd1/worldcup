# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# vanity keys — NEVER commit private keys
/vanity-keys/
*.keypair.json

# generated flag PNGs (run npm run flags to regenerate)
/flags-png/

# Pinata upload result mapping (regenerate with npm run pinata)
/metadata-cids.json

# Deployer artifacts. assignments.json IS committed so the vanity ↔ country
# mapping is stable across machines and pre-allocated on Vercel; deploys.json
# (tx receipts) and vanity-keys (private keys!) are not.
/scripts/deploy/deploys.json
/vanity-keys/
